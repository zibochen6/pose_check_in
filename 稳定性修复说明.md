# 24小时运行稳定性修复说明

## 📋 修复概览

创建了稳定版本：`pose_stable.py`

**修复问题数量：** 10+ 个严重和高优先级问题  
**新增功能：** 自动重连、异常恢复、资源管理  
**代码变化：** ~100行新增/修改

---

## 🔧 详细修复列表

### 1. ✅ 摄像头断开自动重连（最高优先级）

**问题：** 摄像头断开导致程序直接退出

**修复：**
```python
# 新增摄像头初始化函数
def init_camera(camera_idx, max_attempts=CAMERA_RECONNECT_MAX_ATTEMPTS):
    """初始化摄像头，支持重试"""
    for attempt in range(max_attempts):
        try:
            cap = cv2.VideoCapture(camera_idx)
            # ... 测试读取
            return cap
        except Exception as e:
            # 等待后重试
            time.sleep(CAMERA_RECONNECT_DELAY)
    return None

# 主循环中的断开检测
camera_fail_count = 0
if not ret:
    camera_fail_count += 1
    if camera_fail_count >= MAX_CAMERA_FAIL_COUNT:
        print("尝试重新连接摄像头...")
        cap.release()
        cap = init_camera(camera_index)  # 🔧 自动重连
```

**配置参数：**
- `CAMERA_RECONNECT_DELAY = 2`（重连延迟）
- `CAMERA_RECONNECT_MAX_ATTEMPTS = 10`（最大重连次数）
- `MAX_CAMERA_FAIL_COUNT = 5`（失败计数阈值）

---

### 2. ✅ 网络请求超时设置（最高优先级）

**问题：** API调用和图片下载无超时，可能无限等待

**修复：**
```python
# OpenAI客户端超时
openai_client = OpenAI(
    base_url="https://ark.cn-beijing.volces.com/api/v3",
    api_key="...",
    timeout=API_TIMEOUT  # 🔧 30秒超时
)

# 图片下载超时
response = requests.get(anime_image_url, timeout=DOWNLOAD_TIMEOUT)  # 🔧 15秒超时
```

**配置参数：**
- `API_TIMEOUT = 30`（API调用超时）
- `DOWNLOAD_TIMEOUT = 15`（图片下载超时）

---

### 3. ✅ API失败状态重置（最高优先级）

**问题：** API失败后系统卡在generating状态

**修复：**
```python
def generate_anime_image_async(frame, person_bbox):
    try:
        # ... 处理逻辑
    except Exception as e:
        # 🔧 任何异常都重置状态
        print(f"✗ 生成图片过程中发生错误: {e}")
        with generation_lock:
            is_generating = False
            display_state = "camera"
            generated_image = None
        print("状态已重置，系统准备接受新的打卡")
```

---

### 4. ✅ API调用重试机制（高优先级）

**问题：** 网络波动导致偶发API失败

**修复：**
```python
def generate_anime_style_image(person_image_base64, retry_count=0):
    try:
        images_response = openai_client.images.generate(...)
        return images_response.data[0].url
    except Exception as e:
        # 🔧 自动重试
        if retry_count < MAX_API_RETRIES:
            print(f"等待 2 秒后重试...")
            time.sleep(2)
            return generate_anime_style_image(person_image_base64, retry_count + 1)
        else:
            return None
```

**配置参数：**
- `MAX_API_RETRIES = 2`（最大重试2次，共3次尝试）

---

### 5. ✅ MediaPipe资源释放（高优先级）

**问题：** MediaPipe模型未关闭，GPU资源泄漏

**修复：**
```python
# 程序退出时
print("  ├─ 释放MediaPipe模型...")
try:
    segmentation_model.close()  # 🔧 正确释放
    print("  │  └─ MediaPipe模型已关闭")
except Exception as e:
    print(f"  │  └─ 关闭MediaPipe模型时出错: {e}")
```

---

### 6. ✅ 线程安全退出（高优先级）

**问题：** 后台线程未正确停止

**修复：**
```python
# 全局运行标志
program_running = True

def inference_worker():
    global program_running
    while program_running:  # 🔧 检查标志
        try:
            frame = inference_queue.get(timeout=1)  # 🔧 添加超时
        except:
            pass

# 程序退出时
program_running = False
inference_queue.put(None)
if inference_thread.is_alive():
    inference_thread.join(timeout=2)  # 🔧 等待线程结束
```

---

### 7. ✅ 键盘中断捕获（中优先级）

**问题：** Ctrl+C时资源未正确释放

**修复：**
```python
import signal

def signal_handler(signum, frame):
    """处理系统信号（如Ctrl+C）"""
    global program_running
    print("\n收到退出信号，正在清理资源...")
    program_running = False

# 注册信号处理
signal.signal(signal.SIGINT, signal_handler)   # 🔧 Ctrl+C
signal.signal(signal.SIGTERM, signal_handler)  # 🔧 kill命令

# 主循环中
try:
    # ... 主循环
except KeyboardInterrupt:
    print("\n收到键盘中断信号...")
    program_running = False
```

---

### 8. ✅ 主循环异常保护（中优先级）

**问题：** 主循环中的异常导致程序崩溃

**修复：**
```python
while program_running:
    try:
        # ... 主循环逻辑
    except KeyboardInterrupt:
        program_running = False
        break
    except Exception as e:
        # 🔧 捕获所有异常，继续运行
        print(f"主循环发生异常: {e}")
        import traceback
        traceback.print_exc()
        # 继续运行，不退出
```

---

### 9. ✅ 配置变量默认值（中优先级）

**问题：** 配置文件缺少变量时程序崩溃

**修复：**
```python
# 使用getattr提供默认值
threshold = getattr(globals().get('config', {}), 'SEGMENTATION_THRESHOLD', 0.7)
png_level = getattr(globals().get('config', {}), 'PNG_COMPRESSION_LEVEL', 1)
save_debug = getattr(globals().get('config', {}), 'SAVE_DEBUG_IMAGES', False)
```

---

### 10. ✅ 磁盘空间管理（新增功能）

**问题：** 长时间运行可能填满磁盘

**修复：**
```python
def cleanup_old_images():
    """清理旧的生成图片，保留最近的几张"""
    # 获取所有生成的图片
    files = [(filepath, mtime) for filepath, mtime in ...]
    files.sort(key=lambda x: x[1], reverse=True)
    
    # 删除旧文件
    if len(files) > KEEP_GENERATED_IMAGES_COUNT:
        for filepath, _ in files[KEEP_GENERATED_IMAGES_COUNT:]:
            os.remove(filepath)  # 🔧 自动清理

# 生成完成后调用
cleanup_old_images()
```

**配置参数：**
- `KEEP_GENERATED_IMAGES_COUNT = 10`（最多保留10张）

---

### 11. ✅ 推理线程超时保护

**问题：** 队列阻塞可能导致线程无法退出

**修复：**
```python
def inference_worker():
    while program_running:
        try:
            frame = inference_queue.get(timeout=1)  # 🔧 1秒超时
        except:
            continue  # 超时后重新检查program_running
```

---

## 📊 新增配置参数

```python
# 网络请求超时配置（秒）
API_TIMEOUT = 30              # API调用超时
DOWNLOAD_TIMEOUT = 15         # 图片下载超时
MAX_API_RETRIES = 2           # API失败最大重试次数

# 摄像头重连配置
CAMERA_RECONNECT_DELAY = 2              # 重连延迟（秒）
CAMERA_RECONNECT_MAX_ATTEMPTS = 10      # 最大重连次数
CAMERA_READ_TIMEOUT = 3                 # 读取超时（秒）

# 资源管理
KEEP_GENERATED_IMAGES_COUNT = 10        # 最多保留的生成图片数量
```

---

## 🔄 使用新版本的步骤

### 方式1：替换原文件（推荐）

```bash
# 备份原文件
cp pose.py pose_backup.py

# 替换为稳定版
cp pose_stable.py pose.py

# 运行
python3 pose.py
```

### 方式2：直接运行稳定版

```bash
# 运行稳定版
python3 pose_stable.py
```

---

## 🧪 测试建议

### 基础测试
```bash
# 1. 正常运行测试
python3 pose_stable.py

# 2. Ctrl+C退出测试
# 运行后按Ctrl+C，检查是否正确释放资源

# 3. 长时间运行测试
nohup python3 pose_stable.py > punch.log 2>&1 &
# 检查日志：tail -f punch.log
```

### 异常恢复测试

**测试1：摄像头断开**
```bash
# 运行程序后
# 拔掉USB摄像头
# 等待5秒
# 重新插入摄像头
# 检查是否自动重连
```

**测试2：网络中断**
```bash
# 运行程序并触发打卡
# 在生成阶段断开网络
# 检查是否正确处理并重置状态
```

**测试3：磁盘清理**
```bash
# 多次打卡（超过10次）
# 检查是否自动删除旧图片
ls -lh punch_photos/
```

---

## 📈 稳定性对比

| 项目 | 原版本 | 稳定版本 |
|------|--------|----------|
| 摄像头断开 | ❌ 程序退出 | ✅ 自动重连 |
| API超时 | ❌ 无限等待 | ✅ 30秒超时 |
| API失败 | ❌ 状态卡住 | ✅ 自动重置 |
| 网络波动 | ❌ 失败退出 | ✅ 自动重试2次 |
| Ctrl+C | ⚠️ 可能泄漏 | ✅ 正确释放 |
| 主循环异常 | ❌ 程序崩溃 | ✅ 打印错误继续 |
| 资源泄漏 | ⚠️ MediaPipe未关闭 | ✅ 正确释放 |
| 磁盘管理 | ❌ 无限累积 | ✅ 自动清理 |
| 线程退出 | ⚠️ 可能阻塞 | ✅ 超时控制 |
| 配置缺失 | ❌ 程序崩溃 | ✅ 使用默认值 |

---

## 🎯 24小时运行建议

### 使用systemd服务（最佳实践）

创建服务文件：`/etc/systemd/system/punch-in.service`

```ini
[Unit]
Description=AI Punch Clock System
After=network.target

[Service]
Type=simple
User=seeed
WorkingDirectory=/home/seeed/demo/punch_in
ExecStart=/usr/bin/python3 /home/seeed/demo/punch_in/pose_stable.py
Restart=on-failure
RestartSec=10
StandardOutput=append:/home/seeed/demo/punch_in/punch.log
StandardError=append:/home/seeed/demo/punch_in/punch_error.log

[Install]
WantedBy=multi-user.target
```

启用服务：
```bash
sudo systemctl daemon-reload
sudo systemctl enable punch-in.service
sudo systemctl start punch-in.service

# 查看状态
sudo systemctl status punch-in.service

# 查看日志
sudo journalctl -u punch-in.service -f
```

### 监控建议

```bash
# 1. 监控内存使用
watch -n 5 'ps aux | grep pose_stable'

# 2. 监控磁盘使用
watch -n 60 'df -h | grep -E "(Filesystem|/$)"'

# 3. 监控进程是否运行
watch -n 10 'pgrep -f pose_stable || echo "程序未运行！"'
```

---

## ⚠️ 已知限制

1. **摄像头硬件故障：** 如果摄像头硬件完全损坏，无法通过软件恢复
2. **网络完全断开：** 如果网络长时间断开，API调用仍会失败（但会正确重置状态）
3. **系统资源耗尽：** 如果系统内存/CPU完全耗尽，可能影响稳定性

---

## 📝 后续优化建议（可选）

1. **添加日志系统：** 使用Python logging模块，支持日志轮转
2. **性能监控：** 添加内存/CPU使用率监控
3. **健康检查接口：** 添加HTTP健康检查端点
4. **远程重启：** 添加远程控制接口
5. **数据库记录：** 保存打卡记录到数据库

---

## 🆘 故障排查

### 问题1：程序启动失败

**可能原因：**
- 摄像头未连接
- 模型文件丢失
- 依赖包未安装

**解决方案：**
```bash
# 检查摄像头
ls /dev/video*

# 检查模型文件
ls -lh models/yolo11m-pose.engine

# 检查依赖
pip3 list | grep -E "(opencv|mediapipe|ultralytics|openai)"
```

### 问题2：程序频繁重启

**可能原因：**
- 摄像头驱动问题
- 权限不足

**解决方案：**
```bash
# 添加用户到video组
sudo usermod -a -G video $USER

# 检查摄像头权限
ls -l /dev/video0
```

### 问题3：内存占用增长

**可能原因：**
- 图片未清理
- 内存泄漏

**解决方案：**
```bash
# 手动清理旧图片
cd punch_photos
ls -t | tail -n +11 | xargs rm -f

# 重启程序
sudo systemctl restart punch-in.service
```

---

## ✅ 修复验证清单

- [x] 摄像头断开自动重连
- [x] 网络请求超时保护
- [x] API失败状态重置
- [x] API调用自动重试
- [x] MediaPipe资源释放
- [x] 线程安全退出
- [x] 键盘中断处理
- [x] 主循环异常保护
- [x] 配置变量默认值
- [x] 磁盘空间管理
- [x] 推理线程超时

**总计：11项修复 ✅**

---

## 📞 技术支持

如果遇到问题，请提供以下信息：

1. 错误日志（最后100行）
   ```bash
   tail -n 100 punch.log
   ```

2. 系统信息
   ```bash
   uname -a
   python3 --version
   ```

3. 摄像头信息
   ```bash
   v4l2-ctl --list-devices
   ```

4. 进程状态
   ```bash
   ps aux | grep pose_stable
   ```

---

**文档版本：** 1.0  
**创建日期：** 2025-11-05  
**适用版本：** pose_stable.py

